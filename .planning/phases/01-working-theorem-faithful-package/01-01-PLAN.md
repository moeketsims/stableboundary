---
phase: 01-working-theorem-faithful-package
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - pyproject.toml
  - LICENSE
  - src/stableboundary/__init__.py
  - src/stableboundary/py.typed
  - src/stableboundary/_exceptions.py
  - src/stableboundary/parameters.py
  - src/stableboundary/design.py
  - tests/conftest.py
  - tests/test_public_api.py
  - tests/test_parameters.py
  - tests/test_design.py
autonomous: true
requirements: [PKG-02, PAR-01, PAR-02, PAR-03, DES-01, DES-02, DES-03]
user_setup: []
must_haves:
  truths:
    - "A Python 3.12+ environment can install stableboundary from standards-based metadata without importing repository-only files."
    - "Users can construct immutable S0 parameters and convert losslessly between conventional, local, and signed-gap coordinates while retaining r."
    - "Exact-Gaussian conversion refuses to invent beta or p."
    - "A design derived only from n and c satisfies the critical-rate equation, records its theoretical threshold, and rejects unsupported prior rectangles."
    - "Known location and scale carry explicit provenance instead of being silently treated as estimated constants."
  artifacts:
    - path: "pyproject.toml"
      provides: "PEP 621 metadata, Hatchling src-layout build, dependencies, and tool configuration"
      contains: "name = \"stableboundary\""
    - path: "src/stableboundary/parameters.py"
      provides: "Immutable S0 and local-coordinate contracts"
      exports: ["StableParams", "LocalCoordinates", "SignedTailGap"]
    - path: "src/stableboundary/design.py"
      provides: "Critical-rate design, compact prior, and known-nuisance provenance"
      exports: ["LocalDesign", "LocalPrior", "KnownNuisance", "NuisanceMode"]
    - path: "src/stableboundary/py.typed"
      provides: "PEP 561 typing marker"
  key_links:
    - from: "src/stableboundary/design.py"
      to: "scipy.special.lambertw"
      via: "closed-form critical-rate computation"
      pattern: "lambertw"
    - from: "src/stableboundary/__init__.py"
      to: "src/stableboundary/parameters.py"
      via: "curated public exports"
      pattern: "from \\.parameters import"
---

<objective>
Create the installable package foundation and freeze the mathematical contracts for S0 parameters, local coordinates, signed tail gaps, prespecified design, proper compact prior, and known nuisance provenance.

Purpose: Every numerical and inferential layer needs one unambiguous parameterization and a design that cannot adapt to the observations.
Output: Build metadata, typed immutable public objects, domain/refusal errors, and contract tests.
</objective>

<execution_context>
@C:/Users/Moeketsi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Moeketsi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-working-theorem-faithful-package/01-CONTEXT.md
@.planning/phases/01-working-theorem-faithful-package/01-RESEARCH.md
@.planning/phases/01-working-theorem-faithful-package/01-VALIDATION.md
@gaussian_boundary_stable_manuscript.tex
</context>

<threat_model>
  <trust_boundaries>
    <boundary>Caller-supplied numeric values and provenance enter immutable package contracts.</boundary>
    <boundary>Build metadata determines which source files enter installed artifacts.</boundary>
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>P1-T1</threat_id>
      <category>Tampering</category>
      <component>Parameter and design constructors</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Reject nonfinite, out-of-domain, internally inconsistent, and mutable inputs in parameters.py and design.py; verify with property and mutation tests.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>P1-T2</threat_id>
      <category>Spoofing</category>
      <component>Known nuisance provenance</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Use a closed NuisanceMode enum and require nonempty provenance; the Phase 1 fitter accepts only externally_known mode.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the standards-based package and test toolchain</name>
  <files>.gitignore, pyproject.toml, LICENSE, src/stableboundary/__init__.py, src/stableboundary/py.typed, src/stableboundary/_exceptions.py, tests/conftest.py, tests/test_public_api.py</files>
  <read_first>.planning/research/STACK.md, .planning/phases/01-working-theorem-faithful-package/01-VALIDATION.md, AGENTS.md</read_first>
  <action>Create a Hatchling `src/` package named `stableboundary`, version `0.1.0`, requiring Python `>=3.12`, NumPy `>=2.2`, and SciPy `>=1.18`. Add a `dev` extra with pytest, Hypothesis, Ruff, mypy, build, coverage, check-wheel-contents, and twine. Configure pytest with `pythonpath = ["src"]` plus markers `slow` and `installed`, strict mypy for `src`, and Ruff for Python 3.12. Ship `py.typed`, an MIT license, and a `.gitignore` that excludes build, virtual-environment, cache, and LaTeX auxiliary files without ignoring the manuscript, PDF, or planning spike sources. Define separate package errors for validation, unsupported identification, numerical probabilities, convergence, and infinite moments. Curate `__all__`; do not expose private backends. Add import/metadata/error-hierarchy tests. Bootstrap the environment with `python -m pip install -e ".[dev]"` before invoking any test, lint, type, or build command.</action>
  <verify>python -m pip install -e ".[dev]" &amp;&amp; python -m pytest -q tests/test_public_api.py</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `requires-python = ">=3.12"`, the two runtime dependencies, the `src` package mapping, and test/lint/type configuration.
    - `src/stableboundary/py.typed` exists in the wheel package configuration.
    - Importing `stableboundary` exposes a version and package exceptions without importing `.planning` or manuscript code.
    - `python -m pytest -q tests/test_public_api.py` exits zero.
  </acceptance_criteria>
  <done>The package can be imported from the development environment and its public/error contracts are test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Implement immutable S0 and boundary-coordinate objects</name>
  <files>src/stableboundary/parameters.py, src/stableboundary/__init__.py, tests/test_parameters.py, tests/conftest.py</files>
  <read_first>src/stableboundary/_exceptions.py, src/stableboundary/__init__.py, gaussian_boundary_stable_manuscript.tex, .planning/spikes/001-gaussian-boundary-stable/CONVENTIONS.md</read_first>
  <action>Implement frozen typed `StableParams(alpha,beta,loc,scale)`, `LocalCoordinates(r,h,p)`, and `SignedTailGap(r,tau_plus,tau_minus)`. Enforce finite values with alpha in `(0,2]`, beta in `[-1,1]`, positive scale/r/h, and p strictly inside `(0,1)`. Implement the exact identities `alpha=2-r*h`, `beta=2*p-1`, `tau_plus=r*h*p`, and `tau_minus=r*h*(1-p)`, with round-trip constructors that retain `r`. Reject local conversion when `alpha==2` because p/beta is not identified, and reject local mappings whose alpha is not strictly inside `(0,2)`; do not round or clip boundary values. Document that at alpha two the stable variance is `2*scale**2`, so scale is not the Gaussian standard deviation. Export the three types from the public facade. Add example-based and Hypothesis property tests for domains, immutability, identities, reflection (`beta -> -beta` swaps signed gaps), and Gaussian refusal.</action>
  <verify>python -m pytest -q tests/test_parameters.py</verify>
  <acceptance_criteria>
    - `parameters.py` defines the three public frozen dataclasses and no public field named `delta`.
    - Hypothesis round trips preserve alpha, beta, and r within declared floating tolerance.
    - Exact alpha two raises `UnidentifiedParameterError` on conversion to local coordinates.
    - Mutation attempts on public parameter objects fail.
  </acceptance_criteria>
  <done>Parameter domains and all manuscript coordinate identities are executable, immutable, and property-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the prespecified local design, compact prior, and nuisance provenance</name>
  <files>src/stableboundary/design.py, src/stableboundary/__init__.py, tests/test_design.py, tests/conftest.py</files>
  <read_first>src/stableboundary/parameters.py, src/stableboundary/_exceptions.py, gaussian_boundary_stable_manuscript.tex, .planning/phases/01-working-theorem-faithful-package/01-RESEARCH.md</read_first>
  <action>Implement frozen `LocalDesign.from_sample_size(n, c=1.0)` using `r=(8*c/n)*real(lambertw(n/(8*c)))` and `u=2*sqrt(log(1/r)+2*log(log(1/r)))`. Store n, c, r, threshold, formula/version identifiers, and validate finite real outputs plus the critical-rate residual. Implement a proper uniform `LocalPrior` on compact bounds, with documented default `h in [0.25,4.0]` and `p in [0.05,0.95]`; validate positive area, strict interior p, and that the full h support maps to alpha strictly inside `(0,2)`. Provide vectorized `log_density` returning `-inf` outside support. Define the closed enum `NuisanceMode` with exactly `externally_known`, `pilot_conditioned`, and `plugin_estimate`; implement frozen `KnownNuisance(loc,scale,mode,provenance)` requiring finite loc, positive finite scale, a valid enum value, and nonempty provenance. Provide a constructor for external knowledge only; do not call a sample estimator. The Phase 1 fit will accept only `externally_known` and refuse the other recorded modes. Export all four design objects and add tests for the exact equation, threshold, invalid n/c/support, immutable design records, each provenance mode, and Phase 1 refusal semantics.</action>
  <verify>python -m pytest -q tests/test_design.py tests/test_parameters.py</verify>
  <acceptance_criteria>
    - The relative residual of `n*r/log(1/r) - 8*c` is below `1e-12` for tested designs.
    - Every default prior corner maps strictly into the supported stable region.
    - Invalid support, nonfinite inputs, unknown nuisance modes, and missing provenance raise package validation errors.
    - No function in `design.py` accepts observations or chooses a threshold from data.
  </acceptance_criteria>
  <done>A caller can construct and audit a theorem-interior experiment before seeing main-sample cell counts.</done>
</task>

</tasks>

<verification>
- [ ] `python -m pytest -q tests/test_public_api.py tests/test_parameters.py tests/test_design.py`
- [ ] `python -m ruff check src tests`
- [ ] `python -m mypy src`
- [ ] `python -m build` creates a wheel and sdist containing `stableboundary/py.typed`
</verification>

<success_criteria>
- All tasks and targeted tests pass.
- The public package has one explicit S0 convention and no exact-Gaussian beta fiction.
- Design and prior construction are independent of observations and fully auditable.
- Package metadata follows ordinary Scientific Python packaging conventions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-working-theorem-faithful-package/01-01-SUMMARY.md`.
</output>
