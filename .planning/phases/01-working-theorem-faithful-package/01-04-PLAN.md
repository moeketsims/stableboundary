---
phase: 01-working-theorem-faithful-package
plan: "04"
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - README.md
  - examples/known_nuisance_fit.py
  - tests/test_installed_package.py
  - scripts/smoke_wheel.py
  - .github/workflows/ci.yml
  - pyproject.toml
autonomous: true
requirements: [PKG-01, PKG-03, VAL-02]
user_setup: []
must_haves:
  truths:
    - "A new user can follow one documented example to simulate data and complete the exact known-nuisance fit."
    - "The example reports S0, fixed location/scale, signed-gap summaries, identification warnings, and research_uncertified status without implying a general four-parameter estimator."
    - "The built wheel can be installed and exercised from outside the source tree in a clean temporary virtual environment."
    - "Maintainer lint, type, test, build, and wheel-smoke commands are documented and execute successfully."
  artifacts:
    - path: "examples/known_nuisance_fit.py"
      provides: "Fixed-seed public proof of work"
      contains: "fit_known_nuisance"
    - path: "scripts/smoke_wheel.py"
      provides: "Isolated wheel installation and external-path execution"
    - path: "README.md"
      provides: "Scope-honest quickstart and maintainer commands"
      contains: "research_uncertified"
    - path: ".github/workflows/ci.yml"
      provides: "Cross-version automated package checks"
  key_links:
    - from: "examples/known_nuisance_fit.py"
      to: "src/stableboundary/api.py"
      via: "top-level public fit call"
      pattern: "sb\\.fit_known_nuisance"
    - from: "scripts/smoke_wheel.py"
      to: "dist/*.whl"
      via: "temporary-venv pip installation"
      pattern: "venv"
    - from: "tests/test_installed_package.py"
      to: "scripts/smoke_wheel.py"
      via: "installed marker smoke invocation"
      pattern: "smoke_wheel"
---

<objective>
Prove the package works as an installed artifact, not merely from the repository, and document the exact scientific scope of that proof.

Purpose: The user requires a visible fit before submitting the paper; installation isolation and an executable quickstart prevent a scaffold or import-path accident from masquerading as a working package.
Output: README, fixed-seed example, CI, build inspection, and an automated clean-wheel smoke test.
</objective>

<execution_context>
@C:/Users/Moeketsi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Moeketsi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-working-theorem-faithful-package/01-CONTEXT.md
@.planning/phases/01-working-theorem-faithful-package/01-VALIDATION.md
@.planning/phases/01-working-theorem-faithful-package/01-03-SUMMARY.md
@pyproject.toml
@src/stableboundary/__init__.py
</context>

<threat_model>
  <trust_boundaries>
    <boundary>Built archives cross from the working repository into newly created execution environments.</boundary>
    <boundary>The smoke runner launches subprocesses using artifact and example paths.</boundary>
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>P4-T1</threat_id>
      <category>Elevation of privilege</category>
      <component>Artifact smoke runner</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Resolve artifacts under the repository dist directory, reject missing/duplicate archives, use subprocess argument lists with shell disabled, and execute only the repository-owned example in temporary directories.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>P4-T2</threat_id>
      <category>Tampering</category>
      <component>Distribution contents</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Run twine and wheel-content checks and assert forbidden planning, cache, test, and auxiliary files are absent from both archives.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write and test the fixed-seed installed-package quickstart</name>
  <files>examples/known_nuisance_fit.py, README.md, tests/test_installed_package.py</files>
  <read_first>src/stableboundary/__init__.py, src/stableboundary/result.py, .planning/phases/01-working-theorem-faithful-package/01-CONTEXT.md, .planning/research/PITFALLS.md</read_first>
  <action>Create `examples/known_nuisance_fit.py` using only `import stableboundary as sb`: build the design first for n=5000, set truth alpha as `2-design.r*1.5`, simulate with seed 20260824, and complete `fit_known_nuisance` with declared loc/scale and default prior. Print a compact JSON-safe or readable summary containing status, S0, fixed nuisance provenance, counts, alpha/beta and signed-gap intervals, refinement evidence, and warnings. Keep runtime suitable for CI by using documented quadrature settings that still pass refinement. Write README installation, quickstart, interpretation, limitations, S0 scale note, and maintainer sections. State prominently that this is known-nuisance, exact finite-cell, and `research_uncertified`; Phase 1 does not estimate all four conventional parameters or certify the reduction. Add an installed-marker test that executes the exact same example entry point and asserts finite normalized output rather than duplicating a toy path.</action>
  <verify>python examples/known_nuisance_fit.py &amp;&amp; python -m pytest -q -m "not installed"</verify>
  <acceptance_criteria>
    - The example exits zero and prints `research_uncertified`, S0, fixed loc/scale, cell counts, six posterior summaries, and refinement data.
    - The example derives truth from the design rather than hard-coding an alpha that could fall outside the prior support.
    - README contains no claim of certification, safe reduction, or completed four-parameter estimation.
    - `test_installed_package.py` delegates to the artifact smoke runner but is not executed until Task 2 has built both archives.
  </acceptance_criteria>
  <done>The first user workflow is executable, scope-honest, and covered by an end-to-end test.</done>
</task>

<task type="auto">
  <name>Task 2: Add isolated wheel smoke verification and CI</name>
  <files>scripts/smoke_wheel.py, .github/workflows/ci.yml, pyproject.toml, tests/test_installed_package.py</files>
  <read_first>pyproject.toml, examples/known_nuisance_fit.py, .planning/phases/01-working-theorem-faithful-package/01-VALIDATION.md, README.md</read_first>
  <action>Implement a cross-platform Python smoke script that requires exactly one wheel and one sdist in `dist/`, then tests each archive in its own temporary virtual environment. Before installation, inspect wheel members with `zipfile` and sdist members with `tarfile`; assert `stableboundary/py.typed` is present in the wheel and that `.planning`, tests, manuscript auxiliary suffixes, caches, and build outputs are absent from both archives. Install the selected artifact with pip and dependencies, copy the repository-owned example into a temporary directory outside the repository root, run it there, and verify import origin is inside that temporary environment rather than the checkout. Use subprocess argument lists with `shell=False`, propagate nonzero status, and clean temporary directories through context management. Make `tests/test_installed_package.py` invoke this script only under the installed marker. Add CI on Windows, Linux, and macOS for Python 3.12, 3.13, and 3.14 in this strict order: editable dev bootstrap; Ruff/mypy; ordinary pytest with `not installed`; build; `python -m twine check dist/*`; `python -m check_wheel_contents dist/*.whl`; installed-marker artifact smoke. Allow a documented matrix exclusion only if SciPy publishes no wheel for a newly released interpreter. Ensure Hatchling includes `py.typed` and excludes planning, tests, manuscript auxiliaries, and caches. Do not publish a release or upload to PyPI.</action>
  <verify>python -m build &amp;&amp; python -m twine check dist/* &amp;&amp; python -m check_wheel_contents dist/*.whl &amp;&amp; python -m pytest -q tests/test_installed_package.py -m installed</verify>
  <acceptance_criteria>
    - `dist/` contains one wheel and one sdist after a clean build.
    - Explicit zip/tar member-list assertions confirm both archives exclude `.planning`, tests, manuscript auxiliary files, caches, and build outputs; the wheel includes `stableboundary/py.typed`.
    - Separate clean environments install the wheel and sdist; each confirms `stableboundary.__file__` is outside the repository and the complete example exits zero.
    - CI contains lint, strict type, test, build, and isolated wheel-smoke steps.
  </acceptance_criteria>
  <done>The built distribution installs cleanly and runs the real Bayesian example from outside the source tree.</done>
</task>

</tasks>

<verification>
- [ ] `python -m ruff check .`
- [ ] `python -m ruff format --check .`
- [ ] `python -m mypy src`
- [ ] `python -m pytest -q -m "not installed"`
- [ ] `python -m build`
- [ ] `python -m twine check dist/*`
- [ ] `python -m check_wheel_contents dist/*.whl`
- [ ] `python -m pytest -q tests/test_installed_package.py -m installed`
- [ ] Manual claim-language read confirms the README does not overstate Phase 1.
</verification>

<success_criteria>
- The documented fixed-seed exact fit completes and returns finite normalized summaries.
- Wheel and sdist build cleanly and the wheel passes an external-directory fresh-venv smoke test.
- Documentation communicates known nuisance, S0, identification warnings, infinite variance, and research_uncertified status.
- All Phase 1 validation commands pass without importing repository-only code.
</success_criteria>

<output>
After completion, create `.planning/phases/01-working-theorem-faithful-package/01-04-SUMMARY.md`.
</output>
