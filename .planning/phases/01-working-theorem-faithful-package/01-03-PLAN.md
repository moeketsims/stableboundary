---
phase: 01-working-theorem-faithful-package
plan: "03"
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/stableboundary/posterior.py
  - src/stableboundary/result.py
  - src/stableboundary/approximation.py
  - src/stableboundary/api.py
  - src/stableboundary/__init__.py
  - tests/test_fit_known.py
  - tests/test_identification.py
  - tests/test_prediction.py
  - tests/test_approximation.py
autonomous: true
requirements: [FIT-02, FIT-03, FIT-04, FIT-05, FIT-06, FIT-07]
user_setup: []
must_haves:
  truths:
    - "A caller can fit the exact finite three-cell Bayesian posterior for h and p conditional on declared known location and scale."
    - "Deterministic quadrature normalizes in the log domain, retains a read-only weighted grid, records common-grid joint-posterior refinement evidence, and fails if convergence is not demonstrated."
    - "The result summarizes h, p, alpha, beta, tau_plus, and tau_minus and is always labelled research_uncertified."
    - "Zero total tail evidence labels beta prior-dominated; one-sided positive counts report one_sided_evidence, while both one- and two-sided results remain not_assessed for validated precision and expose quantitative information diagnostics."
    - "Posterior tail prediction, expected exceedance counts, and seeded predictive quantiles with Monte Carlo error metadata work, while predictive variance is refused whenever alpha below two has posterior support."
    - "The limiting Gamma-Beta method is an explicitly named approximation and is never invoked by the exact fit path."
  artifacts:
    - path: "src/stableboundary/posterior.py"
      provides: "Deterministic Gauss-Legendre exact finite-cell posterior"
      exports: ["PosteriorGrid"]
    - path: "src/stableboundary/result.py"
      provides: "Immutable summaries, diagnostics, audit, and prediction"
      exports: ["KnownNuisanceFit", "CredibleInterval"]
    - path: "src/stableboundary/api.py"
      provides: "Complete public known-nuisance fitting path"
      exports: ["fit_known_nuisance"]
    - path: "src/stableboundary/approximation.py"
      provides: "Explicit limiting posterior benchmark"
      contains: "approximation"
  key_links:
    - from: "src/stableboundary/api.py"
      to: "src/stableboundary/cells.py"
      via: "observations reduced under the supplied frozen design"
      pattern: "CellCounts"
    - from: "src/stableboundary/posterior.py"
      to: "src/stableboundary/cells.py"
      via: "exact finite cell probabilities at every quadrature node"
      pattern: "exact_cell_probabilities"
    - from: "src/stableboundary/result.py"
      to: "src/stableboundary/backends/_protocol.py"
      via: "full stable posterior-predictive mixture"
      pattern: "StableBackend"
---

<objective>
Complete the first scientifically usable Bayesian vertical slice: exact finite-cell posterior integration, immutable summaries and identification diagnostics, prediction, and a separately labelled limiting benchmark.

Purpose: Demonstrate that the theoretical coordinates yield a working fit before developing the paper's independent-validation and certification claims.
Output: `fit_known_nuisance`, deterministic posterior/result objects, predictive methods, explicit approximation API, and integration tests.
</objective>

<execution_context>
@C:/Users/Moeketsi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Moeketsi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-working-theorem-faithful-package/01-CONTEXT.md
@.planning/phases/01-working-theorem-faithful-package/01-RESEARCH.md
@.planning/phases/01-working-theorem-faithful-package/01-01-SUMMARY.md
@.planning/phases/01-working-theorem-faithful-package/01-02-SUMMARY.md
@src/stableboundary/parameters.py
@src/stableboundary/design.py
@src/stableboundary/cells.py
</context>

<threat_model>
  <trust_boundaries>
    <boundary>Untrusted observations and quadrature controls enter an expensive numerical posterior calculation.</boundary>
    <boundary>Audit and summary outputs may be persisted or consumed by downstream research scripts.</boundary>
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>P3-T1</threat_id>
      <category>Denial of service</category>
      <component>Quadrature and predictive sampling</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Validate bounded integer node/draw counts and finite tolerances before allocation; use deterministic defaults and structured convergence failure.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>P3-T2</threat_id>
      <category>Repudiation</category>
      <component>Scientific result and audit record</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Hard-code research_uncertified, serialize method/config/provenance/warnings, and prohibit silent fallback to the limiting approximation.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement the exact finite posterior on a refinable deterministic grid</name>
  <files>src/stableboundary/posterior.py, tests/test_fit_known.py</files>
  <read_first>src/stableboundary/cells.py, src/stableboundary/design.py, src/stableboundary/parameters.py, src/stableboundary/_exceptions.py, gaussian_boundary_stable_manuscript.tex</read_first>
  <action>Implement immutable `QuadratureConfig(base_nodes=16, refined_nodes=24, refinement_tolerance=0.002, interval_mass=0.90, common_grid_points=65)` and `PosteriorGrid` holding non-writeable two-dimensional h/p nodes, normalized probability masses, log normalizer, node counts, and structured refinement diagnostics. Generate tensor Gauss-Legendre nodes transformed to the exact compact prior rectangle. At each node evaluate exact finite S0 cell probabilities and the multinomial log kernel with `scipy.special.xlogy`; add log prior and log quadrature measure and normalize with `logsumexp`. Never exponentiate raw uncentered log weights. Construct bilinear interpolants of the base and refined log posterior densities on the same 65-by-65 regular h/p grid, normalize each with the same deterministic trapezoidal measure, and compute joint posterior TV as one half of their integrated absolute density difference. Also compare absolute log-normalizer change; support-range-normalized changes in means and 90% interval endpoints for h, p, alpha, beta, tau_plus, and tau_minus; and symmetric relative changes in posterior predictive positive/negative design-tail probabilities. Record every component and raise `ConvergenceError` if any component or joint TV exceeds 0.002. Return the refined grid only. Add analytic uniform/no-data normalization checks, nontrivial count fits, mass-sum/read-only tests, reproducibility, dependence-sensitive joint-TV fixtures, and deliberately too-tight/fixed-node failure tests.</action>
  <verify>python -m pytest -q tests/test_fit_known.py -k "exact or grid or refinement"</verify>
  <acceptance_criteria>
    - Stored posterior mass is finite, nonnegative, read-only, and sums to one within `1e-12`.
    - Exact cell probabilities, not the limiting approximation, appear in the posterior likelihood path.
    - Log-normalizer, common-grid joint TV, signed-gap/parameter summary, and predictive-tail refinement components plus the 0.002 tolerance are retained.
    - Nonfinite normalization and failed refinement raise `ConvergenceError` rather than returning a result.
  </acceptance_criteria>
  <done>The compact exact finite posterior is computed deterministically and refuses unconverged results.</done>
</task>

<task type="auto">
  <name>Task 2: Build immutable results, summaries, identification diagnostics, and prediction</name>
  <files>src/stableboundary/result.py, src/stableboundary/api.py, src/stableboundary/__init__.py, tests/test_fit_known.py, tests/test_identification.py, tests/test_prediction.py</files>
  <read_first>src/stableboundary/posterior.py, src/stableboundary/cells.py, src/stableboundary/simulation.py, src/stableboundary/backends/_protocol.py, .planning/phases/01-working-theorem-faithful-package/01-CONTEXT.md</read_first>
  <action>Implement public `fit_known_nuisance(x,loc,scale,design,prior=None,*,provenance="caller-declared",quadrature: QuadratureConfig | None=None)`. It must validate that `len(x)==design.n`, create `KnownNuisance` in `externally_known` mode, refuse pilot-conditioned/plugin modes in this API, form immutable cells, run the exact posterior, and return frozen `KnownNuisanceFit`. Provide weighted means, medians, equal-tail credible intervals, and a structured `summary()` for h, p, alpha, beta, tau_plus, and tau_minus; always retain r alongside h. Hard-code the only Phase 1 status as `research_uncertified` and expose a versioned JSON-safe `audit_record()` containing package version, S0 convention, known nuisance/provenance, design/prior, counts, quadrature/refinement, backend, RNG metadata when applicable, and warnings. When total tail count is zero set evidence status `prior_dominated` and precision status `unidentified`; when the total is positive but one signed count is zero set evidence status `one_sided_evidence`; otherwise set evidence status `two_sided_evidence`. Precision status remains `not_assessed` for every positive-count result until later calibration. In all cases report quantitative p-marginal prior-to-posterior KL divergence and interval-width contraction, without an `identified` label or arbitrary count cutoff. Implement posterior raw-threshold tail probabilities by integrating direct backend log-CDF/log-SF over grid mass; `expected_exceedance_counts(future_size,threshold)` returns future_size times each posterior predictive signed-tail probability. Implement seeded posterior-predictive draws and quantiles with an immutable estimate recording draw count, seed, bit-generator class, eight-batch quantile Monte Carlo standard error, and requested probability. `predictive_variance()` must raise `InfiniteVarianceError` whenever any positive posterior mass has alpha below two. Tests cover summaries, audit serialization, zero/one/two-sided evidence statuses, precision-not-assessed behavior, information diagnostics, expected counts, prediction reproducibility/MC metadata, and variance refusal.</action>
  <verify>python -m pytest -q tests/test_fit_known.py tests/test_identification.py tests/test_prediction.py</verify>
  <acceptance_criteria>
    - `fit_known_nuisance` completes from a fixed-seed sample and returns finite summaries for all six required quantities.
    - The status literal `research_uncertified` is present and no certified/reduced-safe constructor or enum member exists.
    - Zero-tail fixtures produce prior_dominated/unidentified; one-sided positive fixtures produce one_sided_evidence/not_assessed; two-sided fixtures produce two_sided_evidence/not_assessed; all expose information diagnostics.
    - Tail prediction uses direct backend CDF/SF; predictive variance always refuses for this strictly sub-Gaussian-boundary prior.
    - Expected signed exceedance counts equal future sample size times the corresponding predictive tail probabilities.
  </acceptance_criteria>
  <done>The exact posterior is usable through a direct public API with honest diagnostics, audit data, and full-model prediction.</done>
</task>

<task type="auto">
  <name>Task 3: Add the explicitly separate limiting Gamma-Beta benchmark</name>
  <files>src/stableboundary/approximation.py, src/stableboundary/__init__.py, tests/test_approximation.py</files>
  <read_first>gaussian_boundary_stable_manuscript.tex, .planning/spikes/001-gaussian-boundary-stable/boundary_spike.py, src/stableboundary/api.py, src/stableboundary/result.py</read_first>
  <action>Implement explicitly named `fit_limiting_approximation(counts,design,prior=None)` returning a distinct immutable result with `approximation=True`, method `signed_poisson_gamma_beta_limit`, assumptions, support, and summaries. Pin down the limit as independent counts with `lambda_plus=2*c*h*p` and `lambda_minus=2*c*h*(1-p)` on the same compact rectangle K. For the default uniform prior this gives `h | N` proportional to Gamma(shape=1+N_plus+N_minus, rate=2*c) truncated to the prior h interval and `p | N` proportional to Beta(1+N_plus,1+N_minus) truncated to the prior p interval. Normalize the truncated laws deterministically; do not substitute an unbounded conjugate posterior. Handle zero and one-sided counts without fabricating sign information. Do not import this function in `api.py` and do not call it in `fit_known_nuisance`. Export it from the top-level facade only under the full approximation name. Add intensity, shape/rate, compact-truncation, hand-calculated quadrature, zero-tail, and exact-fitter separation tests.</action>
  <verify>python -m pytest -q tests/test_approximation.py tests/test_fit_known.py</verify>
  <acceptance_criteria>
    - The result contains `approximation is True` and an explicit limiting-method identifier.
    - Intensities, Gamma/Beta parameters, and compact truncation match the manuscript, including zero-tail data.
    - `src/stableboundary/api.py` has no import from `approximation.py`.
    - The default exact fit remains based on finite stable cell probabilities.
  </acceptance_criteria>
  <done>The analytic limit is useful for comparison but cannot silently replace the finite-sample Bayesian method.</done>
</task>

</tasks>

<verification>
- [ ] `python -m pytest -q tests/test_fit_known.py tests/test_identification.py tests/test_prediction.py tests/test_approximation.py`
- [ ] `python -m pytest -q -m "not slow and not installed"`
- [ ] `python -m ruff check src tests`
- [ ] `python -m mypy src`
- [ ] Source inspection confirms the exact API has no limiting-approximation dependency.
</verification>

<success_criteria>
- A fixed-seed sample completes the exact finite Bayesian fit through the public API.
- Posterior normalization and refinement are demonstrated, not assumed.
- Identification limitations and infinite variance are visible refusals/warnings.
- Every result is research_uncertified and the limit remains explicitly separate.
</success_criteria>

<output>
After completion, create `.planning/phases/01-working-theorem-faithful-package/01-03-SUMMARY.md`.
</output>
