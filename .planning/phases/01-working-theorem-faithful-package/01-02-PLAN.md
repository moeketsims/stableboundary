---
phase: 01-working-theorem-faithful-package
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/stableboundary/backends/__init__.py
  - src/stableboundary/backends/_protocol.py
  - src/stableboundary/backends/_scipy_s0.py
  - src/stableboundary/simulation.py
  - src/stableboundary/cells.py
  - src/stableboundary/__init__.py
  - tests/test_probabilities.py
  - tests/test_simulation.py
autonomous: true
requirements: [NUM-01, NUM-02, NUM-04, FIT-01]
user_setup: []
must_haves:
  truths:
    - "Stable log densities, log tails, probabilities, and samples use Nolan S0 without leaving SciPy process-global state changed."
    - "Positive extreme probabilities are obtained by a direct survival call, never by subtracting a CDF from one."
    - "Invalid finite-cell probability vectors fail visibly and are never clipped into the simplex."
    - "A seeded public simulation is reproducible for the installed dependency environment."
    - "Fixed raw observations and a prespecified design produce one immutable negative/central/positive count partition."
  artifacts:
    - path: "src/stableboundary/backends/_protocol.py"
      provides: "Package-owned stable log-density, log-tail, probability, and sampling protocol"
    - path: "src/stableboundary/backends/_scipy_s0.py"
      provides: "Locked snapshot/set/restore S0 bootstrap adapter"
      contains: "parameterization"
    - path: "src/stableboundary/cells.py"
      provides: "Immutable cell counts and exact finite S0 probabilities"
      exports: ["CellCounts", "CellProbabilities"]
    - path: "src/stableboundary/simulation.py"
      provides: "Generator-based reproducible stable simulation"
      exports: ["simulate"]
  key_links:
    - from: "src/stableboundary/cells.py"
      to: "src/stableboundary/backends/_protocol.py"
      via: "backend protocol dependency"
      pattern: "StableBackend"
    - from: "src/stableboundary/backends/_scipy_s0.py"
      to: "scipy.stats.levy_stable.sf"
      via: "direct positive-tail evaluation"
      pattern: "\\.sf\\("
    - from: "src/stableboundary/simulation.py"
      to: "src/stableboundary/backends/_scipy_s0.py"
      via: "default guarded S0 backend"
      pattern: "ScipyS0Backend"
---

<objective>
Implement the guarded S0 numerical boundary, reproducible simulation, and exact finite three-cell experiment on which the Bayesian posterior will operate.

Purpose: Make the likelihood inputs correct, testable, and replaceable before any posterior code can amplify numerical errors.
Output: A package-owned backend protocol, guarded SciPy adapter, simulation API, immutable counts/probabilities, and adversarial numerical tests.
</objective>

<execution_context>
@C:/Users/Moeketsi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Moeketsi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-working-theorem-faithful-package/01-CONTEXT.md
@.planning/phases/01-working-theorem-faithful-package/01-RESEARCH.md
@.planning/phases/01-working-theorem-faithful-package/01-VALIDATION.md
@.planning/phases/01-working-theorem-faithful-package/01-01-SUMMARY.md
@src/stableboundary/parameters.py
@src/stableboundary/design.py
</context>

<threat_model>
  <trust_boundaries>
    <boundary>The package temporarily changes process-global SciPy stable-law configuration.</boundary>
    <boundary>Caller arrays and requested simulation sizes control numerical work and allocation.</boundary>
  </trust_boundaries>
  <threats>
    <threat>
      <threat_id>P2-T1</threat_id>
      <category>Tampering</category>
      <component>Guarded SciPy adapter</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Hold a process lock, snapshot every relevant levy_stable setting, force canonical S0 piecewise settings, and restore all values in finally; test success and exception paths.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>P2-T2</threat_id>
      <category>Denial of service</category>
      <component>Simulation and cell inputs</component>
      <disposition>mitigate</disposition>
      <mitigation_plan>Validate integer sizes, dimensionality, finiteness, and arithmetic overflow before backend calls; never allocate hidden copies repeatedly.</mitigation_plan>
    </threat>
  </threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build the package-owned backend protocol and guarded SciPy S0 adapter</name>
  <files>src/stableboundary/backends/__init__.py, src/stableboundary/backends/_protocol.py, src/stableboundary/backends/_scipy_s0.py, tests/test_probabilities.py</files>
  <read_first>src/stableboundary/_exceptions.py, src/stableboundary/parameters.py, .planning/research/PITFALLS.md, .planning/phases/01-working-theorem-faithful-package/01-RESEARCH.md</read_first>
  <action>Define a runtime-checkable `StableBackend` protocol for `logpdf`, `cdf`, `sf`, `logcdf`, `logsf`, and random sampling over S0 parameters, plus immutable backend metadata naming the numerical method and tolerance. Implement private `ScipyS0Backend` as the only module that imports `scipy.stats.levy_stable`. Guard every SciPy call with a module-level re-entrant lock and a context manager that explicitly snapshots `parameterization`, `pdf_default_method`, `cdf_default_method`, `quad_eps`, `piecewise_x_tol_near_zeta`, `piecewise_alpha_tol_near_one`, `pdf_fft_grid_spacing`, `pdf_fft_n_points_two_power`, `pdf_fft_interpolation_degree`, `pdf_fft_interpolation_level`, and `pdf_fft_min_points_threshold`; force canonical `S0`, piecewise PDF/CDF, and `quad_eps=1.2e-14`, then restore every exact prior value in `finally`, including on exceptions. Do not mutate SciPy at import time and do not expose mutable SciPy distribution objects publicly. Accept scalar and NumPy-array alpha/beta inputs where SciPy broadcasting is supported; finite inputs at supported interior parameter points must produce finite log densities/log tails or a structured package numerical error. Tests must overwrite every guarded setting before a normal call and a forced-error call and prove the complete snapshot is restored. Add spies showing positive-tail requests invoke `logsf`/`sf`, never CDF subtraction.</action>
  <verify>python -m pytest -q tests/test_probabilities.py -k "state or backend or tail"</verify>
  <acceptance_criteria>
    - `scipy.stats.levy_stable` appears only in `_scipy_s0.py` under `src/stableboundary`.
    - The adapter restores every guarded SciPy setting after successful and failing calls.
    - Finite S0 `logpdf`, `logcdf`, and `logsf` evaluations are covered at interior reference points.
    - The positive-tail path calls direct survival/log-survival methods and contains no `1 - cdf` or `1.0 - cdf` expression.
    - Backend failures surface as `ProbabilityError` with operation and parameters in context.
  </acceptance_criteria>
  <done>The package controls stable-law semantics through a replaceable protocol without leaking global state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement seeded S0 simulation and immutable three-cell counting</name>
  <files>src/stableboundary/simulation.py, src/stableboundary/cells.py, src/stableboundary/__init__.py, tests/test_simulation.py, tests/test_probabilities.py</files>
  <read_first>src/stableboundary/backends/_protocol.py, src/stableboundary/backends/_scipy_s0.py, src/stableboundary/parameters.py, src/stableboundary/design.py</read_first>
  <action>Implement public `simulate(params,size,random_state)` using `np.random.default_rng` and the guarded backend; require a positive integer size, reject allocation-overflow requests, and return a finite, one-dimensional, non-writeable float array. Implement frozen `CellCounts(n_minus,n_zero,n_plus,threshold,n)` with a constructor that validates a one-dimensional finite observation array, standardizes exactly once with `KnownNuisance`, and assigns each value to exactly one cell using strict exterior comparisons. Implement frozen `CellProbabilities(q_minus,q_zero,q_plus,log_q_minus,log_q_plus,method,tolerance)` plus `exact_cell_probabilities(local,design,backend)` mapping local coordinates to an explicitly standardized `StableParams(alpha,beta,loc=0,scale=1)`. Obtain signed tails through `logcdf(-u)` and `logsf(u)`, exponentiate only after checking finite logs, reject zero caused by floating underflow because interior finite-threshold tails are strictly positive, derive the center only after direct-tail validation, and reject nonfinite, materially negative, greater-than-one, or non-normalizing values. Permit only a declared `1e-12` simplex tolerance; do not use `clip`, `maximum`, or silent renormalization. Add seed reproducibility, changed-seed, count-sum, threshold-boundary, shift/scale equivariance, reflection, log-tail/probability-normalization, underflow/invalid-input, and read-only-array tests. Export only `simulate`, `CellCounts`, and `CellProbabilities` publicly.</action>
  <verify>python -m pytest -q tests/test_simulation.py tests/test_probabilities.py</verify>
  <acceptance_criteria>
    - Equal integer seeds produce equal arrays; a distinct seed produces a different array.
    - Cell counts always sum exactly to n and values at `-u` and `u` enter the central cell.
    - Reflected beta swaps negative and positive cell probabilities within tolerance, and nuisance shift/scale transformation does not change counts.
    - Zero/underflowed interior tails are refused rather than accepted as valid probabilities.
    - Source inspection finds no clipping or hidden probability renormalization in `cells.py`.
  </acceptance_criteria>
  <done>A deterministic simulated S0 sample can be reduced to a numerically validated exact finite experiment.</done>
</task>

</tasks>

<verification>
- [ ] `python -m pytest -q tests/test_probabilities.py tests/test_simulation.py`
- [ ] `python -m ruff check src tests`
- [ ] `python -m mypy src`
- [ ] A test confirms SciPy's incoming S1 setting survives package calls and exceptions.
</verification>

<success_criteria>
- The S0 adapter is the sole SciPy stable-law access point and restores global state.
- Exact finite-cell probabilities use direct tails, validate the simplex, and never clip.
- Simulation and cell construction are seeded, immutable, and covered by numerical/reflection tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-working-theorem-faithful-package/01-02-SUMMARY.md`.
</output>
