---
quick_id: 260824-w8h
description: "Close final artifact maintenance review gaps: direct sdist install, verifier typing and coverage, single-source build identity"
status: in_progress
tasks: 3
---

# Quick Task 260824-w8h Plan

## Task 1: Test the ordinary source-distribution installation path

- **Files:** `.github/workflows/ci.yml`, `README.md`
- **Action:** Add one explicitly named locked-Linux compatibility step that installs the built sdist through ordinary isolated pip with dependencies and build isolation, runs `pip check`, and imports the installed package from outside the checkout. Distinguish this compatibility check from the authenticated sdist-to-wheel proof in the documentation.
- **Verify:** Workflow syntax remains valid; the direct install/check/import command passes in CI.
- **Done:** The repository no longer claims direct sdist installation without testing it.

## Task 2: Put artifact verifier scripts under strict typing and branch coverage

- **Files:** `pyproject.toml`, `.github/workflows/ci.yml`, `README.md`, `scripts/smoke_wheel.py`, `scripts/generate_artifact_oracle.py`, `tests/test_smoke_wheel.py`
- **Action:** Configure strict mypy for package source and both verifier scripts; repair all type errors. Measure both scripts in the locked coverage run, append oracle execution coverage, retain the package's 80% floor, and enforce a separate 80% scripts floor. Add only meaningful hostile/branch tests if needed.
- **Verify:** `python -m mypy`, Ruff, focused tests, and separate package/scripts branch-coverage reports all pass at 80% or higher.
- **Done:** Critical verifier code is no longer outside the repository's type and coverage gates.

## Task 3: Single-source package and build identity

- **Files:** `pyproject.toml`, `uv.lock`, `scripts/smoke_wheel.py`, `tests/test_smoke_wheel.py`
- **Action:** Pin Hatchling 1.32.0 consistently, retain the trusted hard-coded project-name anchor, derive the version from repository `pyproject.toml`, and test alternate, missing, malformed, and wrong-project metadata. Refresh the lockfile.
- **Verify:** Version-parser tests, focused artifact tests, `python -m build`, lock validation, and the authenticated artifact smoke pass.
- **Done:** A release version or allowed backend update cannot silently desynchronize duplicated verifier constants.

