---
phase: quick
plan: 260824-t4d
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/stableboundary/posterior.py
  - tests/test_fit_known.py
autonomous: true

must_haves:
  truths:
    - "Normalized alpha-mean refinement is exactly equal, bit for bit, to normalized h-mean refinement."
    - "Normalized beta-mean refinement is exactly equal, bit for bit, to normalized p-mean refinement."
    - "A positive primitive-coordinate mean change cannot be erased by cancellation in the affine alpha or beta representation."
    - "The existing scientific tolerance and all non-mean refinement calculations remain unchanged."
  artifacts:
    - path: "src/stableboundary/posterior.py"
      provides: "Cancellation-free affine mean diagnostics in _refinement_diagnostics"
      contains: "def _refinement_diagnostics"
    - path: "tests/test_fit_known.py"
      provides: "Exact-equality and constructed-cancellation regression coverage"
  key_links:
    - from: "src/stableboundary/posterior.py::_refinement_diagnostics"
      to: "SummaryRefinement.mean for alpha and beta"
      via: "Reuse of the normalized h and p mean changes"
      pattern: 'mean_changes\["alpha"\].*mean_changes\["h"\]|"alpha".*"h"'
    - from: "tests/test_fit_known.py"
      to: "src/stableboundary/posterior.py::_refinement_diagnostics"
      via: "A cancellation-prone summary fixture plus an ordinary posterior calculation"
      pattern: "_refinement_diagnostics"
---

<objective>
Make affine posterior mean-refinement diagnostics algebraically stable without relaxing any scientific acceptance threshold.

Purpose: Prevent floating-point cancellation in `2 - r h` and `2 p - 1` from understating refinement and potentially influencing the convergence decision.
Output: A narrowly changed diagnostic implementation and direct regression tests.
</objective>

<execution_context>
@C:/Users/Moeketsi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Moeketsi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@src/stableboundary/posterior.py
@tests/test_fit_known.py

<interfaces>
From `src/stableboundary/posterior.py`:

```python
class SummaryRefinement:
    quantity: str
    mean: float
    median: float
    interval_lower: float
    interval_upper: float

def _refinement_diagnostics(
    base: _GridEvaluation,
    refined: _GridEvaluation,
    design: LocalDesign,
    prior: LocalPrior,
    config: QuadratureConfig,
) -> tuple[RefinementDiagnostics, tuple[_PosteriorSummary, ...]]: ...
```

The affine identities are `alpha = 2 - design.r * h` and `beta = 2 * p - 1`. Because their support ranges are respectively `design.r * h_range` and `2 * p_range`, their normalized mean changes are algebraically identical to those of `h` and `p`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reuse primitive normalized mean changes for affine diagnostics</name>
  <files>tests/test_fit_known.py, src/stableboundary/posterior.py</files>
  <behavior>
    - An ordinary exact-posterior calculation reports `alpha.mean == h.mean` and `beta.mean == p.mean` using exact equality, not an approximate assertion.
    - A constructed base/refined summary pair separates the primitive `h` and `p` means by one representable step while the corresponding floating-point affine means cancel to equality; `_refinement_diagnostics` must still report positive affine changes exactly equal to the primitive changes.
    - Median, interval, joint-total-variation, log-normalizer, predictive-tail, and convergence calculations retain their existing definitions and tolerance.
  </behavior>
  <action>
Follow RED-GREEN-REFACTOR. First add two focused tests to `tests/test_fit_known.py`: (1) compute an exact posterior with the existing analytic test backend, index its refinement summaries by quantity, and assert exact equality for the `alpha`/`h` and `beta`/`p` mean pairs; (2) exercise `_refinement_diagnostics` with controlled `_PosteriorSummary` sequences whose `h` and `p` means differ but whose explicitly evaluated affine means are equal because of cancellation, asserting that all four returned primitive/affine changes are positive and the paired values are exactly equal. Confirm the tests fail against the current direct affine-subtraction implementation.

Then update only the mean component construction in `_refinement_diagnostics`: calculate each normalized primitive `h` and `p` mean change once, use those stored values for `alpha` and `beta`, and calculate `tau_plus` and `tau_minus` means directly as before. Continue to calculate every median and interval component from its own posterior summaries. Do not change `QuadratureConfig.refinement_tolerance`, `_support_ranges`, `RefinementDiagnostics.maximum_component`, convergence logic, public result summaries, or any artifact-oracle file; artifact-oracle integration is explicitly outside this quick task.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_fit_known.py -x</automated>
    <automated>uv run --frozen --extra dev ruff check src/stableboundary/posterior.py tests/test_fit_known.py</automated>
    <automated>uv run --frozen --extra dev mypy src/stableboundary/posterior.py</automated>
  </verify>
  <done>The two affine mean pairs are exactly identical, the cancellation regression remains positive rather than collapsing to zero, all direct posterior tests pass, and no tolerance or unrelated diagnostic is modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundary

Base/refined floating-point posterior summaries cross into a machine-readable convergence decision; cancellation at this boundary can understate numerical disagreement.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260824-T4D-01 | Tampering / integrity | `_refinement_diagnostics` affine mean components | mitigate | Derive affine normalized changes from the primitive h/p values and lock the identity with exact-equality and cancellation-path tests. |
</threat_model>

<verification>
Run the ordinary non-installed suite after the focused task checks:

`uv run --frozen --extra dev pytest -m "not slow and not installed" -x`

Review `git diff -- src/stableboundary/posterior.py tests/test_fit_known.py` and confirm there is no tolerance change and no artifact-oracle edit.
</verification>

<success_criteria>
- `alpha.mean` and `h.mean` are exactly equal in refinement diagnostics.
- `beta.mean` and `p.mean` are exactly equal in refinement diagnostics.
- The constructed cancellation case yields positive primitive and affine mean changes.
- Existing direct and ordinary tests, Ruff, and mypy pass without widening a scientific tolerance.
</success_criteria>

<output>
After completion, create `.planning/quick/260824-t4d-stabilize-affine-posterior-refinement-me/260824-t4d-SUMMARY.md`.
</output>
